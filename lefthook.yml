pre-commit:
  jobs:
    - name: format
      run: npx ultra format --no-error-on-unmatched-pattern --write {staged_files}
      stage_fixed: true
    - name: lint
      run: npx ultra lint --fix --no-error-on-unmatched-pattern {staged_files}
      stage_fixed: true
